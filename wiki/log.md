# Wiki Log

Chronological record of wiki operations.

## [2026-07-20] update | ai-provider-integration — agy drops --sandbox, contained by a project-local read-only deny-gate (m-fix-agy-headless-review)

agy's headless review was structurally broken on macOS: `--sandbox` blocks git's `$TMPDIR`/xcrun-cache write (every git command → `Operation not permitted`) and headless `--print` soft-denies commands (default `request-review`, no interactive approver; `permissions.allow` grants are flaky because they match the literal command string the model emits non-deterministically, and a PreToolUse hook `decision:"allow"` does not preempt the soft-deny). Fix: the wrapper now runs `agy --add-dir "$PWD" --dangerously-skip-permissions --print-timeout 300s -p ...` (no `--sandbox`), CONTAINED by a project-local `.agents/hooks.json` PreToolUse deny-gate. **ai-provider-integration** edits: renamed "Sandbox-Flag Check" → "Read-only-Flag Check" (agy now requires `--dangerously-skip-permissions` AND rejects a stale `--sandbox`; codex `-s read-only` arm unchanged); rewrote the Agy wrapper bullet to the new invocation + the rationale for each flag; added a "Read-only deny-gate" paragraph documenting the new `plugin/templates/agy-readonly-gate.py` (stdlib `decide()`: read-only allowlist + `_METACHAR_RE` + `_DANGEROUS_FLAG_RE` covering `find -exec`/`-fprint0`, `git diff --output`/`--ext-diff`/`--textconv`, `rg --pre`/`--hostname-bin`; fail-closed) and its deployment via `shared_state.ensure_agy_readonly_gate_deployed` (merge-aware, gated on `_agy_enabled`, called from session-start + config_update) + `agy_gate_is_deployed` + the wrapper's whole-entry deep-equal preflight; reframed the mutation check as detection on top of the gate; added two gotchas (agy's machinery now includes the gate; enabling agy writes `.agents/` into the project and read-only-restricts the user's own manual agy runs). Accepted limitation noted: config-driven `git diff` external drivers on an untrusted repo's `.git/config` are not interceptable by the gate. `updated:` → 2026-07-20; added `plugin/templates/agy-readonly-gate.py` + `plugin/hooks/shared_state.py` to sources. Sourced from the m-fix-agy-headless-review diff.

## [2026-07-14] update | issue-tracking-providers + ai-provider-integration — two security defense-in-depth mechanisms (l-fix-security-hardening-residuals)

Captured two new security mechanisms from the 3-finding hardening task (v0.3.36). **issue-tracking-providers**: extended the `IssueTrackingProvider` bullet with `_warn_if_insecure_base_url` — warns (never rejects) at provider construction when `base_url` is non-https and the host is not loopback (localhost / whole `127.0.0.0/8` / `::1` via `ipaddress.is_loopback`), the construction-time counterpart to the SEC-007 write-path guard; best-effort (never raises) with a credential-safe display (prefers `urlparse.hostname` → `scheme://host[:port]`, scheme-less fallback strips after the last `@`) so the warning itself cannot leak a URL-embedded credential (the P2/W1/`/`-in-userinfo leaks fixed across review rounds). **ai-provider-integration**: added an "Untrusted-content fence (`_fence_untrusted`)" subsection under Credential Filter — the credential-filtered `plan_summary` is wrapped (redact-first-then-fence) with a do-not-follow-instructions preamble + `_UNTRUSTED_BEGIN/END` markers, made forgery-proof by a per-call `secrets.token_hex` `id=<nonce>` on the markers + neutralization of any literal base-marker in the body (fence-breakout defense for imported issue bodies). The third finding — a shared `git_operations.validate_branch_name` consolidating the option-injection guard across engine/optimize/MCP git funcs — is a consolidation covered by the module CLAUDE.md, not surfaced as new wiki knowledge. `updated:` → 2026-07-14 on both pages. Sourced from the l-fix-security-hardening-residuals diff (`plugin/hooks/{issue_provider_base,ai_providers,git_operations,protocol_engine,optimize_completion}.py`, provider `__init__`s, `plugin/mcp/{tools/git_operations,helpers/code_review_utils}.py`).

## [2026-07-13] update | hooks-system — state-lock failure direction (m-fix-posttooluse-lock-failure-resilience)

Bug fix: on a flock-less FS (network/synced drive) / permissions / `msvcrt` contention, `_state_lock()` acquisition raised `OSError` and `post-tool-use.py`'s unguarded end-condition throttle killed the PostToolUse hook on every Bash call. **hooks-system**: added a "State-lock failure direction" subsection under the exit-code contract — best-effort consumers (throttle counters + subagent-depth via `_mutate_subagent_depth`) degrade to an UNLOCKED best-effort write on acquisition failure under an `entered` sentinel (set last; separates acquisition failure from a post-body release/`f.close()` OSError so there's no double-write/stale-overwrite); integrity writers (`set_daic_mode`/`set_task_state`/`set_protocol_state`/`edit_state`) stay fail-closed; the two fail-SAFE DAIC call sites (session-start no-protocol fallback, emergency STOP) use the new `ensure_discussion_mode_best_effort()` which degrades to an unlocked write of ONLY the restrictive `"discussion"` mode (closes the stale-`"implementation"` fail-open). Updated the Cross-Hook Shared State bullets (subagent-depth degrade, DAIC-mode writers) and **corrected the stale STOP gotcha** (`is_emergency_stop` is whole-word `\b(?:STOP|SILENCE)\b`, NOT substring — verified against `shared_state._EMERGENCY_STOP_RE`; substrings like `BACKSTOP` don't trigger, but a whole-word `STOP` in a background-agent notification does). `updated:` already 2026-07-13. Shipped as 0.3.31. Sourced from the m-fix-posttooluse-lock-failure-resilience diff (`plugin/hooks/shared_state.py`, `post-tool-use.py`, `session-start.py`, `user-messages.py`, `test/test_post_tool_use_lock_resilience.py`).

## [2026-07-13] update | hooks-system — enforcement-hook fail-open failure direction (h-fix-daic-enforcement-fail-open)

Captured the hybrid failure-direction decision for the PreToolUse enforcement hook, whose crux (only exit 2 blocks; an uncaught traceback exits 1 and silently disables all enforcement) the page's exit-code contract had not yet addressed. `pages/subsystems/hooks-system.md`: added an **Uncaught-exception failure direction** decision block to the exit-code contract — foreseeable corruption fails CLOSED via restrictive reader defaults (`check_daic_mode_raw`/`check_workflow_bypass`/`get_task_state`/`load_config` degrade instead of raising; `ensure_state_dir()` moved inside the `try`, `isinstance(dict)` guard, widened `except`), and unforeseen bugs fail OPEN LOUDLY via a `def main()` + `except Exception → stderr breadcrumb + sys.exit(0)` backstop; **exit 2 was rejected for the backstop** (rejected alternative — the matcher covers `Read|Grep|Bash`, so blocking would brick the session), and `except Exception` (not `BaseException`) lets deliberate `sys.exit(2)` verdicts pass through so the fail-closed guards are not weakened. Noted the `main()` wrapping in the sessions-enforce section, added the two test files to `sources`, `updated:` → 2026-07-13. Also fixed a stale rationale in `plugin/hooks/CLAUDE.md` (the module is now importable since the stdin parse moved into `main()`; the `optimize_completion._audit_path_matches_frozen` sibling comment still cites the old rationale — a noted minor residual). Sourced from the h-fix-daic-enforcement-fail-open diff.

## [2026-07-06] update | mcp-server — FastMCP comes from the `mcp` SDK, not the standalone `fastmcp` package (l-verify-mcp-server-fastmcp-import)

Documented the dependency-provenance fact the research `r-investigate-large-mcp-arg-failures` (Finding F7) had wrong: `server.py`'s `mcp = FastMCP("team-management")` imports `FastMCP` from `mcp.server.fastmcp` — the official **`mcp` SDK** (now declared directly in `plugin/requirements.in` as `mcp>=1.28.0`), not the standalone `fastmcp` PyPI package (imported nowhere; dropped). `mcp==1.28.0` *does* ship `mcp.server.fastmcp` (verified in the dev + both runtime plugin venvs), so there was never a cold-start crash risk — the real fix was declaring `mcp` directly instead of relying on it transitively via `fastmcp`. Dropping the unused `fastmcp` slimmed the hashed lock 79→36 pins (removed `fastmcp` + `fastmcp-slim` + their closure). Page edits: `pages/subsystems/mcp-server.md` — the FastMCP-instance bullet now names the import source + the fastmcp-drop rationale, the inventory drift-guard parenthetical corrected `(no fastmcp import)` → `(no `mcp` import)` to match the test docstring, and `plugin/requirements.in` added to `sources`. `updated:` already 2026-07-06. Sourced from the l-verify-mcp-server-fastmcp-import diff.

## [2026-07-06] update | mcp-server + state-files + configuration-schema + plugin-conversion — per-project provider tokens (m-per-project-provider-tokens)

Retired the OS-keychain `userConfig` token model (global-per-plugin-per-user, so two projects could not use different tokens) in favour of a per-project, user-authored `.claude/state/provider-tokens.json` (git-ignored, `0600`, PROTECTED — the AI cannot read it; keyed by provider name). `plugin.json` no longer declares `userConfig` (v0.3.22, marketplace in sync). `shared_state.resolve_provider_token` now resolves **file → config** (no env/keychain tier; legacy `CLAUDE_PLUGIN_OPTION_*` env-name keys still read as a back-compat fallback). The retired `write_provider_token_bridge` became `ensure_provider_tokens_file` (create-if-absent seeder: template of blank keys + `_comment`, `0600`, never clobbers/deletes, re-applies `chmod 0600` to a user-created file via `_chmod_0600`; `ensure_claude_dir_gitignored` moved into `shared_state.py` for reuse). Page edits: `pages/subsystems/mcp-server.md` (token-flow bullet rewritten file→config), `pages/entities/state-files.md` (`provider-tokens.json` row + secret note), `pages/entities/configuration-schema.md` (tokens live in the file, not `/plugin`), `pages/subsystems/plugin-conversion.md` (resolution + F5 loop); `updated:` → 2026-07-06 on all four. Left the historical log entries and the still-live credential-filter (`ai-provider-integration.md`) / env-first project-root (`issue-tracking-providers.md`) descriptions untouched. Guidance also repointed across the CLAUDE.md set, README, docs/INSTALL, docs/MCP_SERVER, config.md/init.md, and the provider / `issue_status` / `protocol_engine` error strings. Sourced from the m-per-project-provider-tokens diff.

## [2026-07-04] update | specialized-agents + protocol-task — brainstorm dispatch drift + code-review severity/gate-heading contract (m-agent-prompt-cleanup)

The agent-prompt fleet cleanup surfaced two wiki drifts plus one non-obvious contract worth capturing. `pages/entities/specialized-agents.md`: corrected the stale brainstorm-dispatch description — the 6 specialists now dispatch via dedicated `subagent_type`s (`code-architect` / `code-explorer` / `critic` / `user-perspective` / `risk-security-analyst` / `scope-strategist`), NOT `"Plan"` / `"Explore"` / `general-purpose` with pasted `.md` bodies (the sub-protocol no longer pastes agent files, so specialist frontmatter `tools:` / `model:` are live); removed the now-resolved "subagent_type mismatch in brainstorm" gotcha (both the sub-protocol and the module CLAUDE.md agree on dedicated types); and noted the code-review agent rates findings on the shared Critical/High/Medium/Low scale (also adopted by code-cleanliness; critic/risk-security already used it). `pages/protocols/protocol-task.md`: added a gotcha for the non-obvious work-log heading contract — the two completion gates are NOT emoji-symmetric (`validate_no_critical_issues_in_worklog` makes 🔴 optional; `check_code_review_warnings` REQUIRES 🟡), the Critical heading needs a concrete digit (`(0)`, not a literal `(N)`, which fails closed and blocks completion), both the agent template and the sub-protocol §4 template emit emoji headings, and `test/test_code_review_heading_gates.py` drift-guards it. Both pages already `updated: 2026-07-04`. Sourced from the m-agent-prompt-cleanup diff.

## [2026-07-04] update | issue-tracking-providers — provider-layer consolidation + factory unification (m-provider-layer-dedup)

Refreshed `pages/subsystems/issue-tracking-providers.md` after the provider layer consolidated three near-identical `_make_request` copies into one `IssueTrackingProvider._make_request` (built from each subclass's `self.api_base`/`self.headers`/`self.request_timeout`; Jira gained 204/DELETE/PATCH), single-sourced the error-dict→raise boilerplate (`_raise_on_error`, `isinstance(dict)` guard so list-returning MR/PR endpoints pass through), the task-name slugifier (`_slugify`, now Unicode-aware — keeps Cyrillic/CJK, `"task"` fallback), and the first-`# `-line title scan (`_extract_task_title`). Page edits: the Abstraction section now lists the shared request layer + slug/title helpers; Structured Error Reporting attributes the error dict to the shared base `_make_request` + `_raise_on_error`; `import_issue_as_task` `update_mode` is now uniform across all three providers; the `get_{gitlab,github,jira}_sync` factories share one contract (return None + log, not raise); GitLab `default_branch` is a lazy `@property`. `updated:` → 2026-07-04. Also reflected into `plugin/hooks/CLAUDE.md` (Provider Utility Files: consolidated helpers, `_load_mappings` corrupt→`.corrupt-<ts>` backup + raise, jira sync `jira_issue_key or jira_issue_id` fallback, jira `print()`→provider logger). Sourced from the m-provider-layer-dedup diff.

## [2026-07-04] update | protocol-task + specialized-agents — documentation-step CLAUDE.md conciseness discipline (m-doc-step-conciseness)

Reworked the task-protocol documentation-step prompt (`task.json`) and sharpened the `service-documentation` agent so `CLAUDE.md` stays a lean current-state reference, not a task-by-task changelog: edit-in-place / replace-don't-append, prune superseded/duplicated content, and no task-name tags or task enumeration (that history lives in git + the work log; step 4's work log is exempt and keeps full history). Reflected into two pages: `pages/protocols/protocol-task.md` Step 4 gained a "CLAUDE.md is kept lean, not accreted" bullet; `pages/entities/specialized-agents.md`'s service-documentation entry gained the lean/prune/no-tags qualifier. Both `updated:` → 2026-07-04. Sourced from the m-doc-step-conciseness diff.

## [2026-07-04] update | context-preservation — bounded transcript tail-reads (m-statusline-and-test-infra)

Refreshed the `get_context_length_from_transcript` description in `pages/topics/context-preservation.md` against the m-statusline-and-test-infra diff. The reader no longer parses the whole JSONL (which grew to tens of MB and was read on every prompt): it reverse-scans only the last `TRANSCRIPT_TAIL_BYTES` (1 MB) via the shared `_scan_tail_then_full` primitive (append-ordered transcript → last eligible entry == newest, no timestamp compare), with a one-shot full-file fallback when the bounded tail holds no usage AND the file exceeds the window (a single >1 MB final tool-result/paste) — so the auto-compact/statusline hot path stays flat with no blind spot. Noted the sibling `read_last_jsonl_entry` (statusline last-line timestamp/sessionId) shares the same primitive. `updated:` → 2026-07-04. Scope note: this task's other changes are perf/infra hardening documented in the CLAUDE.md files (statusline honest-MCP-segment + tail-read, `bootstrap_mcp.py` stdout hygiene + missing-lock guard, retired `plugin/mcp/test_server.py` → `test/test_mcp_core.py`, `mcp/core/project.py` `'sessions'` marker removal) — no new wiki page warranted.

## [2026-07-03] update | completion-and-git-flow + hooks-system — discard framework-state preservation + transcript-write exception-safety (h-fix-discard-clean-and-windows-transcript)

Two destructive/high-severity fixes reflected into the two affected pages. `completion-and-git-flow.md`: the `discard` flow + its gotcha were updated — `_completion_discard` now runs `git clean -fdx -e team-management/ -e .claude/`, so a discard no longer wipes the framework's own gitignored state (config, `.claude/state/*-mappings.json` task↔issue links, logs) or untracked sibling tasks / custom protocols under `team-management/`; only the task's source work is discarded (accepted nuance: the discarded task's own untracked file may remain as litter; task-scoped `.claude/state/tasks/<task>/` is still removed by `cleanup_task_scoped_state`). `hooks-system.md`: the `task-transcript-link.py` section gained an **exception-safety** paragraph — both transcript-chunk writes now use `encoding='utf-8'` (previously crashed on Windows cp1252), and the post-increment body is wrapped in `try/except Exception` → decrement-on-error + `sys.exit(2)` to block the Task (a blocked Task means no PostToolUse decrement, so the subagent-depth counter can't be double-decremented and an orphaned increment can't silently bypass DAIC). Both pages already at `updated: 2026-07-03`. Sourced from the h-fix-discard-clean-and-windows-transcript diff.

## [2026-07-03] update | context-preservation + specialized-agents + completion-and-git-flow — legacy protocol pointers retired (h-retire-legacy-protocol-pointers)

Retired the four undeployed `plugin/protocols/*.md` files (deleted) and every runtime pointer to them; refreshed the three wiki pages that still described them as live. `context-preservation.md`: the "context-compaction protocol" runbook section was rewritten as **Graceful compaction** — the file is gone, so the auto-compact / 90% directives now steer the model to consolidate via the `logging` agent + `protocol_save_note`, then run native `/compact`, with the PreCompact hook auto-preserving state; dropped `plugin/protocols/context-compaction.md` from `sources:`. `specialized-agents.md`: the context-gathering / context-refinement / logging / service-documentation invocation citations were rewritten from the deleted `protocols/*.md` filenames to the live task-protocol steps / graceful compaction. `completion-and-git-flow.md`: the "legacy task-completion.md carries a deprecation banner" note was corrected to "was deleted" (authoritative path = `sub-protocols/task-completion.md` + `completion_dispatch`). All three `updated:` → 2026-07-03. Sourced from the h-retire-legacy-protocol-pointers diff.

## [2026-07-03] update | llm-wiki-feature — nested pages, read+self-verify, doc-step reminder (m-wiki-nesting-read-verify-doc-reminder)

Self-verified `pages/subsystems/llm-wiki-feature.md` against the m-wiki-nesting-read-verify-doc-reminder diff and refreshed its stale sections: page storage is now nested by category (`wiki/pages/<category>/<slug>.md`; hybrid schema-listed + ingest-proposed categories; flat pages still valid); ingest gains a category-selection step; lint is nested-aware with nine checks (added uncategorized + category-consistency info checks, wiki-root-relative link resolution, cross-category slug collisions allowed); page-format cross-references are wiki-root-relative with code cited by symbol/file (never line numbers); added a **Read + self-verify usage** section for the new ambient consult+verify rule in `CLAUDE.wiki.md` (which replaced the removed ambient `## Update Guidance`) and the `context-gathering` agent's wiki-gated consult-first step; and the `wiki_update_reminder` description was updated to the enriched what / where-category / index+log / skip form. `updated:` → 2026-07-03; `plugin/agents/context-gathering.md` added to `sources:`. NOTE: this repo's own 24 pages remain flat (backward-compatible) — not migrated as part of this task.

## [2026-07-02] update | wiki + CLAUDE.md — current-state docs cleanup (m-docs-stale-description-cleanup)

Swept the maintained docs to describe only the current state. Removed the obsolete DAIC "trigger phrase" mechanism from root `CLAUDE.md` + `plugin/hooks/CLAUDE.md` (the `## Trigger Phrase Processing` section was replaced with a `## User Message Hook` description of what `user-messages.py` actually does; the dead `trigger_phrases` config key was dropped). Deleted `pages/installer-pipeline.md` and rewrote its 7 inbound links (index + architecture-overview, configuration-schema, hooks-system ×2, ai-provider-integration, llm-wiki-feature) to plain current-state prose. Reframed `pages/subsystems/plugin-conversion.md` from a conversion journey (task-#N sequence, verification status, GOTCHA history) into a current-state architecture reference, fixing the stale guidance mechanism (SessionStart `additionalContext` → deploy + `@`-include managed block in the project `CLAUDE.md`). Normalized present-tense MCP-tool refs to the canonical `mcp__plugin_team-management_tm__*` form (`mcp-server`, `specialized-agents`, `plugin-conversion`, index), and corrected the `.claude/hooks` single-source claims to plugin-root-first resolution. Also deleted the orphan `plugin/templates/CLAUDE.md`. Sourced from the m-docs-stale-description-cleanup diff.

## [2026-07-02] update | configuration-schema — new top-level `project_name` key (m-statusline-project-name)

Added `project_name` to the Top-level / identity key catalog: an optional statusline label rendered on line 2 between the open-tasks and MCP segments (`plugin/templates/statusline.py`), read via `shared_state.load_config()` (kept only when a non-empty post-strip string), settable through `config_update` (`plugin/mcp/tools/config.py`), falling back to the project folder name (`PROJECT_ROOT.name`) when empty / unset / whitespace-only / non-string. Also noted that `features.icon_style` selects the new segment's icon glyph. `updated:` → 2026-07-02; `plugin/mcp/tools/config.py` + `plugin/templates/statusline.py` added to `sources:`.

## [2026-06-30] update | plugin-conversion — Windows launcher verified live + user-documented (m-docs-windows-init-requirement)

Follow-up to the launcher fix below: the "post-merge dogfood" is now **confirmed live** — on Windows, running `/team-management:init` (provisions `python3.exe`) then fully restarting Claude Code resolved `python3: command not found`; the plugin-cache update to 0.3.7 ALONE was insufficient. Because the fix is discoverability-gated (skip init → stay broken; dead hooks cannot self-advise), this docs-only task (v0.3.8) makes the Windows init requirement explicit in `README.md` + `docs/INSTALL.md` (concrete marketplace slug `TeamManagementPlugin/team-management-plugin` (since renamed to `TeamManagementPlugin/claude-plugin`); the `python3: command not found` / `'python3' is not recognized` symptom for self-diagnosis) and guards it with `test/test_docs_windows_requirement.py` (7 tests). A zero-touch Node/wrapper launcher redesign was considered and deferred. Updated the Cross-platform launcher subsection (dogfood → verified live); `README.md`, `docs/INSTALL.md`, `test/test_docs_windows_requirement.py` added to sources.

## [2026-06-30] update | plugin-conversion — Windows launcher (`python3` token) cross-platform fix (h-fix-windows-plugin-launch)

Added a "Cross-platform launcher — the `python3` token" subsection to [Plugin Conversion Architecture](pages/subsystems/plugin-conversion.md) (Runtime wiring). The literal `python3` in `hooks.json`/`.mcp.json` fails on Windows (no `python3.exe`); no single static token works on both OSes (modern macOS has no real `python`), manifests have no per-OS branch, the Windows `.py` association is open-not-run, exec-form needs a real `.exe` (a `.cmd` is not direct-spawnable for the MCP server), and there is no self-heal entry point (broken hooks ⇒ session-start never runs). Resolution: keep the manifests on `python3` and have `/team-management:init` §0 provision a real `python3.exe` (copy of `python.exe` into the Python install dir, ahead of `%LOCALAPPDATA%\Microsoft\WindowsApps` Store-alias shadowing). Also recorded `session-start.py::_ensure_statusline_pinned` switching from a literal `python3` pin to `sys.executable`, plus `_is_ours` path-separator normalization for the Windows backslash self-heal. Mechanism validated by a live user Windows probe; full plugin acceptance is a post-merge dogfood. `updated:` → 2026-06-30; `init.md` + `session-start.py` added to sources.

## [2026-06-28] audit + lint | full validity sweep, 23 pages (structure clean · ~25 content fixes · line-refs de-drifted)

Full validity pass after the repo was recreated (user request). **Structural lint: clean** — 23/23 pages indexed (no orphans/missing), all frontmatter complete, all internal links resolve, 15 tags all within the schema vocabulary, nothing stale by date.

**Content-vs-code audit** (7 parallel validators cross-checking every page against `plugin/`): all behavioral prose verified accurate; fixed the factual drifts — MCP tool count 39/7 → **41/8** (added the `tools/config.py` row; `mcp-server` + `architecture-overview`), `specialized-agents` 17 → **15** subagents (gitlab-sync/jira-sync retirement) + corrected the agy-cli "no tmpfiles" gotcha, credential filter 16 → **17** patterns (added `plugin-option`/SEC-003a), `protocol-research` "two discussion steps" → **three**, `state-files` stale `check_daic_mode_bool`/`check_daic_mode` symbols + obsolete `parse_task_frontmatter`-dup gotcha removed, `hooks-system` dropped the deleted `claude mcp list` block and added the two `config_intent_gate.py` bindings, `daic-enforcement` PROTECTED_PATHS `.claude/state` prefix + the 4th (init) whitelist category, `llm-wiki-feature` reframed `CLAUDE.wiki.md` deployed → SessionStart-injected and reconciled schema.md seeding, `architecture-overview` dropped a since-resolved 80/90-vs-75/90 drift example.

**Dead file references** (`plugin-conversion`): `.gitlab-ci.yml`, `test/test_gitlab_ci_config.py`, `docs/brainstorm-results/plugin-conversion.md`, `docs/plugin-verification-report.md` are absent from the recreated tree (intentional per user) — removed from `sources:`/body and the Testing & CI section reworded to "no CI config ships in-tree".

**Line-reference policy change**: stripped all drift-prone `file.py:NNN` / `` (`:NNN`) `` / `(line N)` citations across 22 pages (635 substitutions), keeping the stable symbol/file names. The numbers had drifted ~+20..+650 lines as the code grew since the pages were written; pages now cite symbols, not line numbers, so they no longer re-drift on every code change.

## [2026-06-25] update | plugin-era doc-drift sweep (m-fix-plugin-era-doc-drift)

Swept maintained docs + wiki for stale references to the **deleted** underscored `team_management/` package (Tier 3 of the 2026-06-25 cleanup audit). `CLAUDE.md`: Key Files repointed to `plugin/`, the "Installer System" section rewritten as "Plugin Runtime & Configuration", "Installation Methods" → plugin enablement, "Installation Variants" → "Distribution"; `_PHASE_REGISTRY`/`normalize_command` symbol-homes corrected (`plugin/hooks/ai_providers.py`, `plugin/hooks/hook_utils.py`). `docs/MCP_SERVER.md`, `USAGE_GUIDE.md`, `knowledge/writing-behavioral-content.md` had path + import swaps. Wiki: `team_management/`→`plugin/` across ~21 pages' `sources:` frontmatter + bodies; deleted-installer references dropped or reframed to the current mechanism (hook registration via `hooks.json`, AI-provider + wiki setup via the `/team-management:config` flow, agent delivery via the plugin) or pointed at the RETIRED `installer-pipeline` page; `llm-wiki-feature`'s "Installer scaffolding (WikiConfigurator)" section condensed to config-flow seeding; all `/tm-*` command refs → `/team-management:*`. LEAVE set preserved as intentional history (installer-pipeline, mcp-server version-skew note, plugin-conversion history, this log, plugin/CLAUDE.md + plugin/hooks/CLAUDE.md rename notes). Classification cross-checked by codex (file:line). Known residual, deferred (out of the balanced pass): `CLAUDE.md` still describes the installer as a live subsystem in ~10 embedded feature-prose spots.

## [2026-06-10] update | state-files + protocol-refactoring + hooks-system (hooks hygiene sweep)

Updated three pages for task m-hooks-hygiene-sweep: `state-files` — durable-write section rewritten for the unique `tempfile.mkstemp` temp names (R2-5 lose-update rationale, 0600-perms and SIGKILL-leak trade-offs), lock section extended with the R2-4 daic-mode serialisation (set_daic_mode / edit_state / session-start fallback) and the new locked counter helpers (R2-6), writers table drops the deleted dead DAIC funcs; `protocol-refactoring` — step-5 "lighter gate" framing replaced: gates now aligned with task.json (sentinel + evidence) with verify_tests_pass as the single deliberate omission (exit-0 contract vs baseline-diff design, drift-guarded by test_refactoring_gates_drift.py), Design Decisions bullet rewritten to record why the original "redundant ceremony" theory was rejected; `hooks-system` — emergency stop documented as deliberately transient (N7 decision) with the field-observed notification-text false-trigger and the MCP resync remedy.

## [2026-06-10] update | mcp-server + ai-provider-integration (MCP git/review tooling fixes)

Updated `mcp-server` for task m-fix-mcp-git-review-tooling: `detect_provider` bullet now documents the load_config-before-cache-check ordering (provider changes picked up without server restart — previously the cache pinned the provider for the process lifetime); `parse_mr_pr_url` bullet documents start-anchored patterns (forwarded-URL host spoof closed, scheme-less URLs intentionally unparsed); `validate_branch_name` bullet documents the dotted-branch class + leading-dash rejection (option-injection guard, rule duplicated inline in `git_push`); restore bullet documents exact `stash@{N}` ref pop (interloper protection), per-call timeouts with TimeoutExpired manual-resolution message, and the restored-AND-unstashed success formula (silent data-loss fix). Added a gotcha to `ai-provider-integration`: the agy-cli wrapper's mutation check false-positives under the designed parallel-dispatch pattern (whole-tree snapshots pick up the main agent's concurrent edits) — observed live this task; treat the WARNING as "verify with git diff".

## [2026-06-10] update | installer-pipeline (installer contract fixes)

Updated `installer-pipeline` for task m-fix-installer-contract-breaks: dependency check is now Python ≥ 3.10 (gotcha "Python version floors disagree" removed — fixed in source); new copy rule deploys `config.template.json` to the path session-start.py's recovery guidance references, and the two dead templates (`settings.disabled.template.json`, `.mcp.json.template`) are deleted; `_save_config` chmods config.json to 0600 on POSIX (R2-2, token-bearing file); dedup section rewritten — `normalize_command` is module-level with symmetric per-token quote stripping via `shlex.split(posix=False)`, plus the new healing collapse pass that removes already-accumulated duplicate hook entries (same matcher + normalized command set, first wins); new gotcha documents `team_management.hooks` becoming a real package (`__init__.py` + pyproject declaration — the `team-workflow` console script was broken from wheel installs, masked locally by implicit namespace packages).

## [2026-06-10] update | ai-provider-integration (M3/M4/M9/R2-3 hardening)

Updated `ai-provider-integration` for task m-harden-ai-provider-layer: credential filter section rewritten (12 → 16 patterns split into value-format and name-in-assignment-context families, with both live prose-redaction incidents recorded; new stateful PEM pass with first-match-wins-independent arming and the accepted END-before-BEGIN gap); "Sandbox-Flag Assertion" section renamed to "Sandbox-Flag Check" (`_ensure_sandbox_flags` + dedicated `SandboxFlagError(ValueError)`, narrow re-raise rationale incl. the JSONDecodeError/UnicodeDecodeError subclass trap that regressed twice in review); wrapper section documents the shell-native agy watchdog on the no-gtimeout branch (background + mktemp + detached sleep/kill subshell, rc>=128 stderr marker, drift-guard test). Touched stale "sandbox-flag assertion" phrasing in `protocol-engine`, `specialized-agents`, and the index line (also fixed its stale "Codex/Gemini" naming).

## [2026-06-10] update | issue-tracking-providers (B1 idempotency + B2 GitLab id/iid)

Updated `issue-tracking-providers` for task m-fix-issue-tracking-integration-robustness. The page's "IID vs ID" bullet and the GitLab gotcha previously said `issue_status` filters on / surfaces the global `gitlab_issue_id` — now stale. Both producers (`create_gitlab_issue_from_task` return + `issue_status` display) now surface the project `iid`; the global `id` is stored but never surfaced (surfacing it 404'd `issue_set_status`/`issue_comment`). Added a new gotcha: `create_issue_from_task` is non-idempotent on its own and the protocol engine's `_func_create_issue_if_enabled` now guards it via `get_task_mapping` (per-provider issue-id key) to prevent duplicate issues on investigation-step re-runs. Added `protocol_engine.py` to the page sources.

## [2026-05-31] ingest | team_management/ (package source)

Deep multi-agent ingest of the `team_management/` package (~36K LOC, 165 files) into 16 cross-referenced pages, followed by a coherence-review pass. Created:

- Overview: `architecture-overview`
- Subsystems: `hooks-system`, `protocol-engine`, `mcp-server`, `installer-pipeline`, `ai-provider-integration`, `issue-tracking-providers`, `llm-wiki-feature`
- Topics: `daic-enforcement`, `context-preservation`
- Entities: `workflow-protocols`, `specialized-agents`, `optimize-protocols`, `state-files`, `configuration-schema`
- Procedure: `completion-and-git-flow`

Coherence review found 3 low-severity issues (all fixed: a double-negative in `workflow-protocols`, a stale `_PHASE_REGISTRY` line reference in `configuration-schema`, a `Gotochas` typo in `protocol-engine`). Pages flagged several genuine doc/code drifts in the framework itself (stale `__version__`, 80/90-vs-75/90 token thresholds, `server.py` "9 protocol engine tools" comment, `gitlab-sync` referencing a non-existent MCP tool) — captured in the relevant pages' Gotchas sections, not yet fixed in source.

## [2026-05-31] lint | 0 critical, 0 warning, 0 info

Clean. 16 pages on disk, all 16 indexed (no orphans/missing). All frontmatter complete, all internal links resolve, 14 distinct tags all within the schema vocabulary, nothing stale.

## [2026-05-31] ingest | per-protocol detail pages

Expanded protocol coverage so each of the 6 shipped workflow protocols has its own detailed page (was: one catalog page `workflow-protocols` + the optimize-pair page `optimize-protocols`). Created 6 entity pages:

- `protocol-task` — 5-step lifecycle, the two-stage code-review gate, per-step funcs/modes/gates.
- `protocol-brainstorm` — 6-specialist analysis + conflict loop, the planning-step traceability audit.
- `protocol-research` — no-branch knowledge workflow, the plain archive chain (not `completion_dispatch`).
- `protocol-refactoring` — test-baseline/test-verify gates, per-increment RED-GREEN, the lighter code-review.
- `protocol-optimize` — interactive batched mode, the `batch_checkpoint` modulo gate.
- `protocol-optimize-unattended` — autonomous twin, termination conditions, Ctrl-C-vs-abort.

Refactored `workflow-protocols` from a per-protocol catalog into a **hub**: shared JSON step schema, `@sub-protocols` resolution, the two-stage gate, family-level design decisions + gotchas, and a catalog table linking to the 6 pages. `optimize-protocols` retained as the shared engine-mechanics reference, now cross-linked from both per-protocol optimize pages. Added a new `## Protocols` section to `index.md` (moved `workflow-protocols` + `optimize-protocols` out of `## Entities`). Sourced from the 6 protocol JSONs + their sub-protocol markdown under `team_management/protocol-configs/`. Net: 22 pages.

## [2026-06-10] update | optimize-protocols (l-optimize-robustness-cleanup)

Updated `optimize-protocols` from the robustness-cleanup task: bounded `metric_parser` matching (`_bounded_regex_search` killable child process — the GIL gotcha that disproved the daemon-thread design), symmetric-at-zero stability gate, `_audit_path_matches_frozen` component-boundary audit matching, `_squash_to_best` HEAD-capture abort + step-1 rollback, and two new gotchas (GIL/re match timeout; restart preserves baseline by design). Sourced from `team_management/hooks/optimize_completion.py`.

## [2026-06-22] update | specialized-agents, daic-enforcement, hooks-system, architecture-overview, completion-and-git-flow (m-retire-dead-agents-and-daic-cleanup)

Retired the `gitlab-sync` / `jira-sync` agents (completion is driven by `completion_dispatch`, not named agents; deployed copies cleaned up via `FilesystemManager.LEGACY_RETIRED_FILES`) and removed the legacy `daic`-substring Bash shim from `sessions-enforce.py` that false-triggered on any discussion-mode command containing `daic` (e.g. `2>/dev/null` reads). Updated the four pages that documented the false-trigger as a live quirk to record the fix, and the specialized-agents / completion-and-git-flow pages to mark the two agents retired. Sourced from `sessions-enforce.py`, `installer/filesystem.py`, `test/test_sessions_enforce_daic.py`.

## [2026-06-24] create | plugin-conversion (m-plugin-verification #6)

New page `plugin-conversion` consolidating the pip→Claude Code plugin conversion architecture (tasks #2–#6): the three-root path model (PROJECT_DIR / PLUGIN_ROOT / PLUGIN_DATA, resolved at point of use), runtime wiring (hooks `_shim.py`, MCP cold-start `bootstrap_mcp.py` venv build, boot-detector, SessionStart guidance injection), the two namespacing schemes, token continuity, and the #6 verification status. Records the two load-bearing gotchas surfaced by the #6 verification gate: (1) plugin-bundled MCP tools are namespaced `mcp__plugin_<plugin>_<server>__<tool>` — NOT `mcp__team-management__*` (documented, unavoidable; behavioral surface still references the legacy form → `h-mcp-tool-namespace-refs`); (2) `IssueTrackingProvider._find_project_root` ignored `CLAUDE_PROJECT_DIR` (empty config in a real install) — fixed env-first here. Fixed the stale "39 tools / 7 modules" count in the `mcp-server` index catalog line → 41/8. Sourced from `docs/brainstorm-results/plugin-conversion.md`, `docs/plugin-verification-report.md`, `plugin/hooks/shared_state.py`, `plugin/mcp/bootstrap_mcp.py`, `plugin/hooks/issue_provider_base.py`.

## [2026-06-24] update | plugin-conversion (h-mcp-tool-namespace-refs)

Updated `plugin-conversion` for the namespace-refs task. The MCP-tools Namespacing bullet was a GOTCHA recording the doubled `mcp__plugin_team-management_team-management__*` form; now RESOLVED — the `plugin/.mcp.json` server key was shortened `team-management` → `tm`, so the canonical plugin-runtime form is `mcp__plugin_team-management_tm__<tool>`. The task swept all 27 `plugin/` files off the legacy `mcp__team-management__*` form, renamed the `.mcp.json` key, added a drift-guard (`test/test_mcp_tool_namespace.py`, forbids the bare + doubled forms across `plugin/`), and Layer-B-verified the `_tm__` form is live in a real plugin runtime without inference. `boot_detector.py`'s legacy-install probe and `server.py`'s `FastMCP("team-management")` name are intentionally untouched (neither drives the tool prefix). Updated the task-sequence line to insert `h-mcp-tool-namespace-refs` between #6 and #7, and refreshed the index catalog line. Sourced from `plugin/.mcp.json`, `test/test_mcp_tool_namespace.py`, `team-management/tasks/h-mcp-tool-namespace-refs.md`.

## [2026-06-25] update | plugin-conversion + installer-pipeline (m-installer-retirement)

Recorded plugin-conversion task #7 (installer retirement) as DONE and added a **RETIRED** banner to `installer-pipeline` (kept as historical record). The pip/npm installer — `plugin/installer/` (27 modules), `plugin/install.py`, the `install.js`/`install.sh` wrappers, and the 4 `[project.scripts]` console scripts — was deleted; pip/npm distribution is dead. Its functions now live in the plugin runtime: hooks via `plugin/hooks/hooks.json`, MCP cold-start via `plugin/.mcp.json` → `bootstrap_mcp.py`, configuration via `/team-management:config` + `/plugin` userConfig, guidance injected at SessionStart. `normalize_command` survives in `plugin/hooks/hook_utils.py`, consumed only by `boot_detector.py`. `team-workflow full disable/enable` was also retired (only `bypass`/`status` remain). Index line annotated RETIRED. Sourced from the m-installer-retirement diff (`pyproject.toml`, `MANIFEST.in`, `plugin/hooks/workflow_command.py`, the deleted `plugin/installer/` tree).

## [2026-06-25] update | plugin-conversion (l-test-suite-ci — #8, conversion COMPLETE)

Recorded plugin-conversion task #8 (tests/CI) as DONE — **the 8-task conversion is now complete**. Updated the intro ("being converted" → "fully converted, all 8 tasks DONE"), frontmatter (added `testing` tag + the two new sources), the task sequence (#8 marked DONE), and added a **Testing & CI** section. New deliverables documented: the F1–F7 plugin-API regression suite (`test/test_plugin_api_regression.py`, single auditable fact→contract map; only F1 and F5 were genuine net-new coverage, the rest aggregate existing per-subsystem suites), the GOTCHA that F1 cross-source hook merge + F2 `CLAUDE_PROJECT_DIR`-in-env are live-host facts pytest cannot prove (spike-verified, our side pinned), the GitLab CI pipeline (`.gitlab-ci.yml`, py310/py313 matrix, `.[mcp]` install, no token literal), and the `test/test_gitlab_ci_config.py` anti-leak drift-guard standing in for live "CI green" (deferred to first push). Suite gate: 937 passed / 24 subtests / 0 failures. Sourced from the l-test-suite-ci diff.

## [2026-06-26] update | plugin-conversion (m-fix-init-settings-whitelist)

Added an **init write-gate GOTCHA** to the Runtime wiring section: `/team-management:init` runs while the plugin is already enforcing, so its writes to `<project>/.claude/settings.json` and `<project>/CLAUDE.tm.custom.md` were blocked by `sessions-enforce.py` (discussion-mode Write block + branch-enforcement no-task block) — observed live as init falling back to a mode-switch + Bash heredoc. Fix: both paths added to the administrative whitelist (exits before both gates) via exact resolved-path equality (set membership; `.bak` sibling / same-named dir does NOT match), so init now writes in-tool with no mode-switch/shell-escape. Security unchanged (settings.json not in PROTECTED_PATHS, Bash already ungated, plugin hooks from hooks.json). Sourced from `plugin/hooks/sessions-enforce.py`, `test/test_sessions_enforce_daic.py`.

## [2026-07-03] update | mcp-server + hooks-system (h-fix-mcp-manager-split-and-notebookedit)

Two silently-broken defects fixed; recorded the durable invariants in both pages. **MCP Server**: added a GOTCHA — after the provider layer split, MR/PR/release methods live on `GitLabMRManager`/`GitLabReleaseManager`/`GitHubPRManager`/`GitHubReleaseManager` (re-exported from `gitlab_utils`/`github_utils`), NOT on bare `GitLabAPI()`/`GitHubAPI()`; the code_review/git_operations/release tools + `helpers/code_review_utils.py` were calling moved methods on bare API instances → swallowed `AttributeError` → tools looked implemented but never worked (16 call sites; `GitHubAPI.add_comment`/`_make_request` were the genuine non-moved exceptions). **Hooks System**: fixed drift in the event-binding matcher table (`sessions-enforce.py` matcher now includes `NotebookEdit`) and added a GOTCHA — the matcher must list every tool the hook special-cases (`{Read,Grep,Edit,Write,MultiEdit,NotebookEdit,Bash}`) or that tool gets zero enforcement; `NotebookEdit` had silently escaped DAIC/protected-path/frozen-path/branch enforcement. Both invariants are drift-guarded (`test/test_mcp_manager_split.py`, `test/test_hooks_matcher_drift.py`). Bumped both pages' `updated:` + sources. Sourced from the task diff.

## [2026-07-06] update | protocol-task + protocol-engine (m-mcp-arg-convention-and-validate-on-skip)

Documented the write-file-first task-delivery convention and the `create_task_file` skip-path hardening. **protocol-engine**: added a GOTCHA — `_func_create_task_file` with empty `task_content` + an existing on-disk file no longer trusts existence alone; it re-runs the new shared `_validate_task_file_structure` validator (frontmatter `---`/closing `---`/status/branch-mismatch/prefix/`## Success Criteria`) at STRICT PARITY with the inline path, blocking the advance on a malformed file (`post_funcs_stop_on_failure`), and NEVER mutates the file (a missing `**Author:**` → a `warnings` entry, not injection; inline injects, skip warns). `_validate_advance_args` still grants the empty-content exception on existence, so the two layers cooperate. **protocol-task**: reframed step-1 "alignment before advance" so write-file-first (write the file, advance with `task_content=""`) is the preferred delivery for substantial task files — large object-typed MCP args drop to `{}`/`__unparsedToolInput` above ~2KB (the upstream, unfixable root cause from research `r-investigate-large-mcp-arg-failures`) — and noted that write-file-first deliberately trips `git_setup_branch`'s dirty-tree pause (it runs before `create_task_file`, so the pre-written file dirties the tree when `team-management/` is tracked) → re-advance with `carry_changes: true` (`research` has no `git_setup_branch`, so it never pauses). Both pages' `updated:` bumped to 2026-07-06. Sourced from the task diff (`plugin/hooks/protocol_engine.py`, the three sub-protocols + `task.json`/`research.json` end-text, `plugin/mcp/tools/protocol.py`).

## [2026-07-07] update | state-files + context-preservation + architecture-overview (l-dead-code-removal)

Drift-fix after the Half-A dead-code removal trimmed `task_state_manager.py` to its live surface. **state-files**: rewrote the "Per-task state directories" section — the multi-session scaffold (`session.json` CRUD / `create_task_state`, `check_parallel_session`, per-task 75/90 context-warning flags, `get_task_by_branch`, and the duplicate module-level `get_task_state_manager`) was deleted; `TaskStateManager` is now three methods — `get_task_state_dir` (guard strengthened this task: rejects empty/whitespace, any name resolving to the `tasks/` root via `.`/`./`/`..`, and `../` traversal, closing the hazard where a bad name let `cleanup_task_state` `rmtree` the whole tree), `get_transcripts_dir`, and `cleanup_task_state` (returns `False`, never raises). Dropped the now-unwritten `tasks/<task>/session.json` + `context-warning-{75,90}.flag` File-Catalog rows, and corrected the `protocol-logs/<task>.json` writer from the deleted `save_protocol_log` to the `ProtocolEngine` audit log. **context-preservation**: the "80/90 vs 75/90 flag mismatch" gotcha is now moot — the task-scoped 75/90 family was removed, so only the global 80/90 path remains. **architecture-overview**: `tasks/<task>/` now holds subagent transcript staging only (not `session.json` / context-warning flags). All three `updated:` bumped to 2026-07-07. Sourced from the l-dead-code-removal diff (`plugin/hooks/task_state_manager.py`).

## [2026-07-11] update | mcp-server + configuration-schema + architecture-overview (m-telegram-chat-id-discovery)

New feature: Telegram `chat_id` discovery for `/team-management:config`. **mcp-server**: tool count 41→42, `tools/notifications.py` row 2→3 (added `notification_discover_telegram_chats`), added `notification_utils.py`/`notifications.py` to sources. **configuration-schema**: documented the discovery mechanism under Notifications — the new MCP tool validates the token with `getMe`, does a NON-mutating `getUpdates` read, and returns the distinct recently-active chats (Telegram has NO endpoint that lists a bot's groups; a chat surfaces only if it recently messaged the bot or the bot was just added via `my_chat_member`; a webhook → 409); the config flow presents chats, sends a confirmation ping to the chosen one before saving `chat_id` (confirm-before-save), and the bot token is never passed to the tool (read from the per-project token file; discovery read-only/ungated, only the write is gated). **architecture-overview**: MCP-tool count 41→42. All three `updated:` bumped to 2026-07-11. Sourced from the m-telegram-chat-id-discovery diff (`plugin/hooks/notification_utils.py`, `plugin/mcp/tools/notifications.py`, `plugin/commands/config.md`).

## [2026-07-12] update | configuration-schema + mcp-server (m-fix-telegram-tls-verification)

Bug fix: Telegram TLS verification failed on a python.org Python whose OpenSSL CA store was never initialised (empty trust store → every chain reads as self-signed; no proxy/MITM) — which is what the plugin's own MCP runtime venv uses. **configuration-schema**: added the new optional `notifications.channels.telegram.ca_bundle` key to the notifications list, and a "Telegram TLS verification" bullet — all Telegram calls now build their SSL context via `notification_utils._telegram_ssl_context()` (precedence: `ca_bundle` / `SSL_CERT_FILE` / `REQUESTS_CA_BUNDLE` → platform default *iff* it has trust anchors → `certifi`, already shipped in the venv via `requests` → `/etc/ssl/cert.pem` → last-resort default; never raises), and `_telegram_api_call` now returns `(payload, error, kind)` classifying TLS/transport failures distinctly from HTTP auth (a cert failure gets a CA-fix hint — Install Certificates.command / `ca_bundle` — never "token rejected"). **mcp-server**: noted the TLS-context + error-classification behavior under `notification_discover_telegram_chats`. `configuration-schema` `updated:` → 2026-07-12. Shipped as 0.3.29. Sourced from the m-fix-telegram-tls-verification diff (`plugin/hooks/notification_utils.py`, `plugin/mcp/tools/notifications.py`, `plugin/mcp/tools/config.py`, `plugin/templates/config.template.json`, `plugin/commands/config.md`).

## [2026-07-13] update | mcp-server (m-fix-mcp-config-cache-poisoning)

Bug fix: the long-lived MCP server permanently cached an empty config after a first file-absent `load_config()` (the fresh-install order — a provider tool touches config before `/team-management:config` writes it), because the missing-file and exception cache paths left `_config_mtime = None`, so the mtime-invalidation guard never fired. **mcp-server**: rewrote the `load_config()` bullet — invalidation is now existence-AND-mtime (one guarded `stat()`; `-1.0` sentinel for a missing/unreadable file, distinguishable from any real mtime ≥ 0; every cache state stamps `_config_mtime`), so a `config.json` created OR deleted after first load is picked up on the next call; the exception path caches the malformed file's own mtime so a stably-broken file isn't re-parsed each call yet self-heals on the next change. Also noted `config_update` now calls `reload_config()` (canonical `from core import config`) after its durable write so a same-session config/provider change takes effect without an MCP restart, and updated the "Caches persist" gotcha (test must reset `_config`/`_config_mtime`/`_provider`, and use the `core.config` module identity — not `plugin.mcp.core.config` — to stay in sync with the server). `updated:` → 2026-07-13. Shipped as 0.3.32. Sourced from the m-fix-mcp-config-cache-poisoning diff (`plugin/mcp/core/config.py`, `plugin/mcp/tools/config.py`, `test/test_mcp_core.py`, `test/test_config_mcp.py`).

## [2026-07-14] update | state-files, issue-tracking-providers (m-fix-mappings-lock-free-rmw)

Bug fix: the provider `*-mappings.json` files were read-modify-written with no `_state_lock` from two processes (the PostToolUse auto-sync hook + the long-lived MCP server), so a concurrent RMW silently lost one writer's update (`_save_mappings`→`_write_json_durable` is atomic, so no torn files — but the load→mutate→save was not serialised). **state-files**: corrected the "Lock only `current_task.json`" gotcha (mappings are now locked too — it was factually wrong after this change), added a "Cross-process write lock" paragraph on the mappings RMW + the two-marker degrade (stricter than the counter helpers because `_save_mappings` doesn't swallow its own `OSError`: acquisition failure → one unlocked best-effort RMW; body `OSError` under the held lock → propagate; post-persist release failure → return the persisted result; corrupt-file `RuntimeError` → propagate), and updated the mappings-file Writer column to `IssueTrackingTaskSync._locked_mapping_update`. **issue-tracking-providers**: added the locked-RMW note to Mapping storage (cross-ref State Files) and noted `_ensure_mappings_file` now seeds via an atomic create-only `open('x')`. Both `updated:` → 2026-07-14. Shipped as 0.3.33. Sourced from the m-fix-mappings-lock-free-rmw diff (`plugin/hooks/issue_provider_base.py`, `plugin/hooks/{gitlab,github}/task_sync.py`, `plugin/hooks/jira_utils.py`, `test/test_mappings_lock_free_rmw.py`).

## [2026-07-14] update | hooks-system, context-preservation (m-fix-unbounded-transcript-reads)

Bug fix (robustness): two transcript readers on hot paths still read the WHOLE session JSONL (tens of MB in long sessions) — `get_model_from_transcript` did `readlines()` then inspected only the last 10 entries (every UserPromptSubmit + auto-compact monitor), and `task-transcript-link.py` loaded + tiktoken-encoded the full transcript on every Task/Agent dispatch inside a blocking PreToolUse hook. Both escaped the earlier m-statusline-and-test-infra tail-read sweep. **context-preservation**: corrected the now-false `get_model_from_transcript` bullet ("scans the last 10 entries" → bounded 1 MB tail-read via `_scan_tail_then_full`; now finds the model even >10 entries back). **hooks-system**: added a "Bounded staging read" paragraph to the `task-transcript-link.py` section (caps staging at `TRANSCRIPT_STAGE_CAP_BYTES` = 1 MB via `_read_file_tail(return_capped=True)`; skips the pre-work strip loop when `capped` to avoid starving the subagent; import hoisted above the depth increment; graceful-degrade on an unreadable transcript). Both `updated:` → 2026-07-14. Underlying: `_read_file_tail` gained `return_capped` (the capped decision comes from the same descriptor observation — no `os.path.getsize` TOCTOU) + a strictly-bounded `f.read(size - read_from)`; new `_newest_model_in_lines`; cap value 1 MB (chosen by Max). Deferred non-blocking refinement (codex P2): strip-if-marker-found / keep-verbatim-if-absent would tighten over-inclusion on a capped read whose boundary lands in-window. Shipped as 0.3.34. Sourced from the m-fix-unbounded-transcript-reads diff (`plugin/hooks/shared_state.py`, `plugin/hooks/task-transcript-link.py`, `test/test_transcript_tail_read.py`, `test/test_transcript_stage_cap.py`).

## [2026-07-15] update | completion-and-git-flow (m-fix-completion-strands-without-remote)

Bug fix: a fresh plugin project with no `issue_tracking` section AND no git remote stranded the completion protocol — `_read_issue_provider` classified the missing key as `("unknown","legacy")` → provider-driven chain, whose `git_merge_main`/`git_push` hard-failed on `git fetch origin`/`git push origin` (no remote), leaving the task committed + archived but unmerged/uncleaned; the user's explicit `merge_local` was ignored. **completion-and-git-flow**: rewrote the provider-detection section — `("disabled","configured")` is now reached two ways (explicit OR inferred: no `issue_tracking.provider` key + no provider enabled, via the new `_any_issue_provider_enabled` gate mirroring `mcp/core/config.py`); an enabled-provider legacy config or a non-dict (corrupt) `issue_tracking` still falls through to the provider chain. Added the no-remote safety net to the provider-chain step + Gotchas (`_origin_remote_exists()` in `protocol_engine.py`; `git_merge_main`/`git_push` return `action:"skipped"` on a demonstrably-absent origin, but a `git remote` error returns True so real failures still surface), reframed the "legacy/unreadable fall through" design decision, and updated the "menu never forced" gotcha. `updated:` → 2026-07-15. Shipped as 0.4.2. Sourced from the m-fix-completion-strands-without-remote diff (`plugin/hooks/optimize_completion.py`, `plugin/hooks/protocol_engine.py`, `test/test_completion_workflow.py`, `test/test_default_branch_detection.py`).

## [2026-07-15] migrate | nested pages by category + schema Categories rule (m-wiki-nest-pages-into-categories)

Reformatted this repo's own wiki from the flat `wiki/pages/<slug>.md` layout into nested category subdirectories `wiki/pages/<category>/<slug>.md`, completing the migration deferred in the 2026-07-03 m-wiki-nesting-read-verify-doc-reminder entry ("this repo's own pages remain flat — not migrated as part of this task"). **schema.md**: added the missing `## Categories` section (between `## Page Types` and `## Page Format Example`), pulled from the current source template `plugin/templates/wiki-schema.md` and tailored to the six index groupings — `overview`, `subsystems`, `protocols`, `topics`, `entities`, `procedures`; the custom Focus / Page-Types / Tag-Conventions / Ingest-Focus / Excluded-Topics sections were preserved unchanged. `CLAUDE.wiki.md` was already byte-identical to its source template, so no rule change there. **pages**: all 22 pages `git mv`'d into their category dir per the index groupings (1 overview / 7 subsystems / 8 protocols / 2 topics / 3 entities / 1 procedure). **links**: 180 wiki-root-relative `pages/<slug>.md` references rewritten to `pages/<category>/<slug>.md` across index.md, all pages, and these log entries (167 markdown links + 13 backtick/prose refs); the 3 anchored `optimize-protocols.md#…` links kept their anchors; the descriptive `pages/<category>/slug.md` placeholders in `llm-wiki-feature.md` were left untouched. Verified: 22 git renames, 0 broken internal links (168 checked), no flat-form link remaining, and the change scoped to `wiki/` only (no plugin/source/version change — the wiki is a dev knowledge base, not a shipped artifact). No page BODY content was edited beyond link paths.
