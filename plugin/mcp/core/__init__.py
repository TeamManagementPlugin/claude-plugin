"""
Core utilities for MCP server.

Provides project detection and configuration management.

Import directly from submodules:
    from core.project import get_project_root, setup_provider_imports
    from core.config import load_config, detect_provider
"""

# Note: Re-exports removed to support running server.py as script.
# Import directly from core.project, core.config instead.
